apiVersion: apps/v1
kind: Deployment
metadata:
  name: lab-db
  namespace: healthcare
  labels:
    app: lab-db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: lab-db
  template:
    metadata:
      labels:
        app: lab-db
    spec:
      containers:
        - name: postgres
          image: postgres:15-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: "lab_db"
            - name: POSTGRES_USER
              value: "postgres"
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: healthcare-secrets
                  key: db-password
            - name: PGDATA
              value: "/var/lib/postgresql/data/pgdata"
          volumeMounts:
            - name: lab-db-storage
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: lab-db-storage
          hostPath:
            path: /data/healthcare/lab-db
            type: DirectoryOrCreate

---

apiVersion: v1
kind: Service
metadata:
  name: lab-db-svc
  namespace: healthcare
spec:
  selector:
    app: lab-db
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP