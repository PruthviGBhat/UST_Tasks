apiVersion: apps/v1
kind: Deployment
metadata:
  name: appointment-db
  namespace: healthcare
  labels:
    app: appointment-db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: appointment-db
  template:
    metadata:
      labels:
        app: appointment-db
    spec:
      containers:
        - name: postgres
          image: postgres:15-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: "appointment_db"
            - name: POSTGRES_USER
              value: "postgres"
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: healthcare-secrets
                  key: db-password
            - name: PGDATA
              value: "/var/lib/postgresql/data/pgdata"
          volumeMounts:
            - name: appointment-db-storage
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: appointment-db-storage
          hostPath:
            path: /data/healthcare/appointment-db
            type: DirectoryOrCreate

---

apiVersion: v1
kind: Service
metadata:
  name: appointment-db-svc
  namespace: healthcare
spec:
  selector:
    app: appointment-db
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP