sudo ln -s /etc/nginx/sites-available/flaskapp /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx


server {
    listen 80;
    server_name _;

    
    location = / {
        return 200 "Nginx is up. Use /testing\n";
        add_header Content-Type text/plain;
    }

    # Proxy /testing (and deeper paths) to Flask
    location /testing {
        proxy_pass http://127.0.0.1:5000;  # no trailing path here
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# See if anything is holding port 5000
sudo ss -ltnp 'sport = :5000' || true
sudo lsof -i :5000 -sTCP:LISTEN || true



# Verify port is now free (should show nothing)
sudo ss -ltnp 'sport = :5000' || true
``

