apiVersion: apps/v1
kind: Deployment
metadata:
  name: pharmacy-db
  namespace: healthcare
  labels:
    app: pharmacy-db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pharmacy-db
  template:
    metadata:
      labels:
        app: pharmacy-db
    spec:
      containers:
        - name: postgres
          image: postgres:15-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: "pharmacy_db"
            - name: POSTGRES_USER
              value: "postgres"
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: healthcare-secrets
                  key: db-password
            - name: PGDATA
              value: "/var/lib/postgresql/data/pgdata"
          volumeMounts:
            - name: pharmacy-db-storage
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: pharmacy-db-storage
          hostPath:
            path: /data/healthcare/pharmacy-db
            type: DirectoryOrCreate

---

apiVersion: v1
kind: Service
metadata:
  name: pharmacy-db-svc
  namespace: healthcare
spec:
  selector:
    app: pharmacy-db
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP