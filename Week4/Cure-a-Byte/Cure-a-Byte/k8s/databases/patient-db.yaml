apiVersion: apps/v1
kind: Deployment
metadata:
  name: patient-db
  namespace: healthcare
  labels:
    app: patient-db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: patient-db
  template:
    metadata:
      labels:
        app: patient-db
    spec:
      containers:
        - name: postgres
          image: postgres:15-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: "patient_db"
            - name: POSTGRES_USER
              value: "postgres"
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: healthcare-secrets
                  key: db-password
            - name: PGDATA
              value: "/var/lib/postgresql/data/pgdata"
          volumeMounts:
            - name: patient-db-storage
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: patient-db-storage
          hostPath:
            path: /data/healthcare/patient-db
            type: DirectoryOrCreate

---

apiVersion: v1
kind: Service
metadata:
  name: patient-db-svc
  namespace: healthcare
spec:
  selector:
    app: patient-db
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP